#pragma once

namespace Sorts
{
    void insertSort(int arr[], const int left, const int right);

    void quickSort(int a[], const int left, const int right);

    void quickSortOptimization(int a[], const int left, const int right);

    int maxbit(int data[], int n);
    void radixSort(int data[], int n);

    void radixSortOptimization(int a[], const int arrayLength);
}